import { ChangeColorOfHeroPipe } from './change-name-of-hero.pipe';

describe('ChangeColorOfHeroPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeColorOfHeroPipe();
    expect(pipe).toBeTruthy();
  });
});
